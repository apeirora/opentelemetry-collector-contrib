type: signing

status:
  class: processor
  stability:
    alpha: [ logs ]
  distributions: [ contrib ]
  warnings: []
  codeowners:
    active: []

tests:
  config:


telemetry:
  metrics:
    signing_processor_processed_logs:
      description: Number of signed log records.
      stability:
        level: development
      unit: "{records}"
      enabled: true
      histogram:
        value_type: int
