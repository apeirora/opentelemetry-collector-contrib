display_name: Signing Processor
type: signing

status:
  class: processor
  stability:
    alpha: [ logs ]
  distributions: [contrib]
  warnings: []
  codeowners:
    active: [MJarmo, hilmarf, jmacd]

tests:
  config:
    key_source:
      type: file
      file:
        certificate: testdata/test-cert.pem
        private_key: testdata/test-key.pem
